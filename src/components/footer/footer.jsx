
import styles from "./footer.module.css";

export const Footer = () =>{

    return(
        <footer>
            <code className={styles.footer}>
                Avec TaskFlow tu as éléminé X tache"s"
            </code>
        </footer>
    )
}